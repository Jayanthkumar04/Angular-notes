import { ActualpricePipe } from './actualprice.pipe';

describe('ActualpricePipe', () => {
  it('create an instance', () => {
    const pipe = new ActualpricePipe();
    expect(pipe).toBeTruthy();
  });
});
